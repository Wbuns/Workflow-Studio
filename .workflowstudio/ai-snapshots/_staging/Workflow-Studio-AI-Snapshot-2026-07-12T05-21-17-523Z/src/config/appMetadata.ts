export const appMetadata = {
  name: "Workflow Studio",
  version: "2.0.1",
  releaseName: "Project Workspace",
  technologyLabel: "Electron + React",
} as const;

export const appVersionLabel = `v${appMetadata.version} ${appMetadata.releaseName}`;
