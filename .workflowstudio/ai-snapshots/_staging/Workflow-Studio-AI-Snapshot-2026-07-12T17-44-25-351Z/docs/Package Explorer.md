# Package Explorer

Workflow Studio now presents `_packages` and `_backup` as an expandable folder tree with breadcrumb navigation. The explorer remains read-only and opens selected files or folders through the existing secure workspace path bridge.
