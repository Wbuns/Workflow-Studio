# Workflow Studio Development Pipeline

**Version:** 2.0.5.4

Adds a reviewed package pipeline that installs through the standard backup-aware installer, runs the detected safe build command, captures output, stops on failure, and leaves the final Git commit under developer control.
