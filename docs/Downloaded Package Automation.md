# Downloaded Package Automation

Workflow Studio can now locate the newest `workflowstudio-*.zip` package in Downloads or accept a selected ZIP. The Electron boundary extracts and copies manifest-approved files while preserving backups. No package script is executed automatically.
