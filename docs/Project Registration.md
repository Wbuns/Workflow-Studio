# Project Registration

The Projects page now manages the Workspace Registry directly. Users can add existing projects, activate projects, favorite or unfavorite them, refresh health through Workspace Intelligence, and remove registry entries without deleting project files.

The registry remains the single source of truth and existing workspace scanning logic is reused.
