# Build Automation Console

Developer Tools now invokes the active project's detected safe build command through the existing secure command bridge. Output is streamed into an in-app console; Workflow Studio remains open when a build fails.

Package installation actions now start the detected build automatically.
