# Active Project and Snapshot Reliability

Workflow Studio now restores the active project stored in the Workspace Registry instead of registering the application folder as active on every launch.

AI snapshots are staged in the operating system temporary directory. Snapshot archives and history remain stored in the selected project's `.workflowstudio/ai-snapshots` folder.
